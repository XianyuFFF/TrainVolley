# TODO change it to significative


def detected_ball_json_dir(video_dir, output_path, cam_id):
    return video_dir + output_path + cam_id

def get_camera_info_dir(cam_id, cam_name):
    return cam_id+cam_name